
pluginManagement {
    resolutionStrategy {
    eachPlugin {
        if (requested.id.id == "net.minecraftforge.gradle") {
            useModule("${requested.id}:ForgeGradle:${requested.version}")
            useModule("org.apache.logging.log4j:log4j-api:2.11.+")
            useModule("org.apache.logging.log4j:log4j-core:2.11.+")
        }
    }
    }
    repositories {
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
        maven { // name = "Garden of Fancy" // 
            url = "https://gitlab.com/api/v4/projects/26758973/packages/maven" // 
        }
        maven {
            url = "https://files.minecraftforge.net/maven/"
        }
        maven { url="https://maven.mcmoddev.com/"}
        maven { url="file:///home/runner/work/SumeruMod/SumeruMod/forge_gradle"}
        maven { url="https://maven.minecraftforge.net/"}
    }
}
rootProject.name = 'SumeruMod'
